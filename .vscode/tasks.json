{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build DAP",
            "type": "shell",
            "command": "gcc -g -Og mir.c -o mir-intp-dap.exe -DMIR_DAP -DMIR_DIRECT_DISPATCH -I.",
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}